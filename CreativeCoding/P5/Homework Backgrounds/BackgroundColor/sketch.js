function setup() {
  //creates the window width and height to be the size of my browser
  createCanvas(windowWidth, windowHeight);
  //gives RGB red background
  background(255, 100, 0);
}

function draw() {

}
